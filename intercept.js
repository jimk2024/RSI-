const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({ args: ['--no-sandbox'] }); const page = await browser.newPage(); page.on('request', request => { if (request.url().includes('hyperliquid.xyz/info')) { console.log(request.method(), request.url(), request.postData()); } }); await page.goto('https://app.hyperliquid.xyz/leaderboard', { waitUntil: 'networkidle2' }); await browser.close(); })(); 