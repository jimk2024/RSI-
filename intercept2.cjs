const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({ args: ['--no-sandbox'] }); const page = await browser.newPage(); const client = await page.createCDPSession(); await client.send('Network.enable'); client.on('Network.webSocketFrameSent', ({response}) => { console.log('WS SENT:', response.payloadData); }); client.on('Network.webSocketFrameReceived', ({response}) => { console.log('WS RECV:', response.payloadData.substring(0, 100)); }); await page.goto('https://app.hyperliquid.xyz/leaderboard', { waitUntil: 'networkidle2' }); await browser.close(); })(); 